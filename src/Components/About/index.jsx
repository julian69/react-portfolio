import React from 'react';
import PropTypes from 'prop-types';

const About = (props) => {
    return (
        <div className="section">
            <h1>About</h1>
        </div>
    );
};

export default About;
