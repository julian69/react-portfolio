import React from 'react';
import PropTypes from 'prop-types';

const Contact = (props) => {
    return (
        <div className="section">
            <h1>Contact</h1>
        </div>
    );
};

export default Contact;
