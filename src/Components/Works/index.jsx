import React from 'react';

import Slider from '../Common/Slider'

const Works = (props) => {
    return (
        <div className="section">
            <Slider />
        </div>
    );
};

export default Works;
