import React from 'react';
import PropTypes from 'prop-types';

const Skills = (props) => {
    return (
        <div className="section">
            <h1>Skills</h1>
        </div>
    );
};

export default Skills;
